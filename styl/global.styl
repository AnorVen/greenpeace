body {
  reset();
}

body {
  margin: 0;
  font-size: 35px;
  line-height: normal;
  font-family: "Merriweather", "Helvetica", "Arial", sans-serif;
  color: white;
  width: 100%;
  position: relative;
}

a {
  text-decoration none;
  color: #f5f5f3;
}

.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  margin: -1px;
  padding: 0;
  border: 0;
  clip: rect(0 0 0 0);
  overflow: hidden;
}
